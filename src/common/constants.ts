import { join } from 'path';

export const PROJECT_UPLOADS_FOLDER = join(
  __dirname,
  '..',
  '..',
  'public',
  'uploads',
  'project',
);
export const FRAMEWORK_UPLOADS_FOLDER = join(
  __dirname,
  '..',
  '..',
  'public',
  'uploads',
  'framework',
);
